# configs\sql_generator_prompts.yaml
# Промпты для SQL Generator моделей

Qwen2.5-Coder-0.5B:
  system: |
    Ты модель для преобразования вопросов в SQL.
    Выводи ТОЛЬКО SQL запрос.

  user_template: |
    Схема БД: {schema}
    Запрос: {query}
    Твой SQL код:

Qwen2.5-Coder-1.5B:
  system: |
    Ты модель для преобразования вопросов в SQL.
    Выводи ТОЛЬКО SQL запрос.

  user_template: |
    Схема БД: {schema}
    Запрос: {query}
    Твой SQL код:

Qwen2.5-Coder-3B:
  system: |
    Ты модель для преобразования вопросов в SQL.
    Выводи ТОЛЬКО SQL запрос.

  user_template: |
    Схема БД: {schema}
    Запрос: {query}
    Твой SQL код:

Qwen2.5-Coder-3B-Instruct:
  system: |
    Ты модель для преобразования вопросов в SQL.
    Выводи ТОЛЬКО SQL запрос.

  user_template: |
    Схема:
    {schema}

    Вопрос: {query}

    SQL:

sqlcoder-7b-2:
  system: |
    You are a text-to-SQL expert. Output ONLY valid SQL.

  user_template: |
    Schema: {schema}
    Question: {query}
    SQL:

sqlcoder-7b-2-instruct:
  system: |
    You are an advanced SQL model. Output ONLY SQL.

  user_template: |
    Schema: {schema}
    Question: {query}
    SQL:
